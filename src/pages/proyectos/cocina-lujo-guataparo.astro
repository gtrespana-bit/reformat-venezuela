---
import BaseLayout from '../../layouts/BaseLayout.astro';
import GuaranteeMatrix from '../../components/GuaranteeMatrix.astro';
import ProjectLightbox from '../../components/ProjectLightbox.astro';
import ProjectQuickNav from '../../components/ProjectQuickNav.astro';
import ProjectPhaseGallery from '../../components/ProjectPhaseGallery.astro';

const project = {
  title: 'Cocina de Lujo en Guataparo',
  kicker: 'Caso real · Cocinas de alto estándar',
  location: 'Quinta residencial · Guataparo, Valencia, Carabobo',
  excerpt:
    'Reubicación integral de una cocina que vivía cerrada e independiente: la trasladamos a la zona día, condenamos su antiguo acceso, renovamos electricidad y fontanería, sustituimos el termo eléctrico por placas solares térmicas, vertimos un suelo epóxico blanco continuo y montamos una composición lineal de muebles bajos con estantería flotante iluminada y equipos Siemens.',
  heroImage: '/images/proyectos/cocina-lujo-guataparo/resultado-final-cocina-lineal.webp',
};

const heroFacts = [
  { label: 'Ubicación', value: 'Guataparo · Valencia' },
  { label: 'Duración', value: '2 semanas y media' },
  { label: 'Intervención', value: 'Reubicación integral' },
  { label: 'Suelo', value: 'Epoxi blanco continuo' },
  { label: 'Equipos', value: 'Siemens · Nevera 90 cm' },
  { label: 'Archivo', value: '16 fotos reales' },
];

const quickNavItems = [
  { href: '#resumen', label: 'Resumen' },
  { href: '#resultado', label: 'Resultado' },
  { href: '#paso-a-paso', label: 'Paso a paso' },
  { href: '#comparativa', label: 'Antes y después' },
  { href: '#especificaciones', label: 'Especificaciones' },
  { href: '#galeria', label: 'Galería' },
];

const whatsappText =
  'Hola, quiero una valoración técnica para una cocina como la de la quinta en Guataparo.';
const whatsappHref = `https://wa.me/584227997043?text=${encodeURIComponent(whatsappText)}`;

const stats = [
  { value: '06', label: 'Fases documentadas' },
  { value: '16', label: 'Fotografías reales' },
  { value: '2,5', label: 'Semanas de ejecución' },
  { value: '0', label: 'Juntas visibles en el suelo' },
  { value: '90', label: 'cm de nevera importada' },
];

const resultadoShowcase = [
  {
    src: '/images/proyectos/cocina-lujo-guataparo/resultado-final-cocina-lineal.webp',
    alt: 'Cocina lineal terminada con muebles bajos blancos, estantería flotante y nevera Siemens en quinta de Guataparo',
    badge: 'Cocina lineal',
    title: 'Una sola línea de mobiliario bajo, coronada por luz oculta',
    caption:
      'Solo muebles bajos —sin módulos altos— para una lectura limpia y minimalista, con la estantería flotante iluminada recorriendo toda la cocina y la nevera Siemens de 90 cm cerrando la composición.',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/resultado-final-comedor-abierto.webp',
    alt: 'Zona día unificada con suelo epóxico blanco continuo, comedor y cocina abierta en quinta de Guataparo',
    badge: 'Zona día unificada',
    title: 'Cocina, comedor y sala leídos como un solo espacio',
    caption:
      'El suelo epóxico blanco sin juntas une visualmente toda la planta: la antigua cocina cerrada se convierte en el corazón abierto de la zona día, bañado de luz natural.',
  },
];

const phaseNav = [
  { number: '01', label: 'Diagnóstico' },
  { number: '02', label: 'Demolición' },
  { number: '03', label: 'Instalaciones' },
  { number: '04', label: 'El suelo' },
  { number: '05', label: 'Montaje' },
  { number: '06', label: 'Entrega' },
];

const phases = [
  {
    number: '01',
    kicker: 'Fase 01 · Diagnóstico',
    title: 'El punto de partida: una cocina aislada que cortaba la casa',
    body:
      'Durante años, la cocina de esta quinta vivió aparte: un compartimento cerrado al que se entraba por una gran puerta doble, con cerámica y mobiliario que ya contaban demasiado tiempo. No conversaba con el comedor ni con la sala; simplemente interrumpía la planta. El diagnóstico fue claro desde la primera visita: no había nada que retocar. Había que mover la cocina entera —y con ella, la forma de vivir la casa—.',
    checks: [
      'Diagnóstico del estado de mobiliario, cerámica e instalaciones',
      'Replanteo completo de la distribución de la zona día',
      'Nueva ubicación definida, integrada con sala y comedor',
    ],
    images: [
      {
        src: '/images/proyectos/cocina-lujo-guataparo/antes-acceso-doble-sala.webp',
        alt: 'Zona día original de la quinta con el acceso doble a la cocina cerrada al fondo',
        caption: 'La zona día antes de la obra: el comedor terminaba en la puerta doble que llevaba a la cocina cerrada.',
        badge: 'Antes',
        orientation: 'landscape',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-2.webp',
        alt: 'Cocina original cerrada vista desde su acceso doble',
        caption: 'El acceso doble: así se entraba a la cocina original, un mundo aparte dentro de la casa.',
        badge: 'Antes',
        orientation: 'portrait',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-4.webp',
        alt: 'Interior de la cocina original con ventana y mobiliario antiguo',
        caption: 'Cerámica, cenefa y mobiliario envejecido: el interior de la cocina tal como se encontró.',
        badge: 'Antes',
        orientation: 'portrait',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-3.webp',
        alt: 'Detalle de la encimera y los módulos antiguos de la cocina original',
        caption: 'Encimera y módulos antiguos, ya fuera de servicio, poco antes del desmontaje.',
        badge: 'Antes',
        orientation: 'portrait',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-1.webp',
        alt: 'La cocina original vista desde su propia entrada',
        caption: 'Un espacio compartimentado e incomunicado, visto desde su propia entrada.',
        badge: 'Antes',
        orientation: 'portrait',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/antes-nueva-ubicacion-comedor.webp',
        alt: 'Comedor original que se convertiría en la nueva ubicación de la cocina',
        caption: 'El comedor original: la habitación elegida para recibir la nueva cocina abierta.',
        badge: 'Antes',
        orientation: 'portrait',
      },
    ],
  },
  {
    number: '02',
    kicker: 'Fase 02 · Demolición controlada',
    title: 'Vaciar para empezar: desmontaje total y adiós al termo',
    body:
      'El primer gesto fue vaciar. Se desmontó la cocina completa —muebles, encimera y equipos— y la antigua habitación quedó en su esqueleto, lista para empezar de cero. En ese punto salió también de escena el termo eléctrico que abastecía a la vieja cocina: se desmontó y quedó condenado. Desde esta obra, el agua caliente de la quinta se produce con placas solares térmicas: menos consumo eléctrico permanente, más eficiencia cada día.',
    checks: [
      'Desmontaje completo de mobiliario, encimera y equipos',
      'Retirada y condena del termo eléctrico existente',
      'Agua caliente preparada para placas solares térmicas',
    ],
    images: [
      {
        src: '/images/proyectos/cocina-lujo-guataparo/obra-desmontaje-termo-electrico.webp',
        alt: 'Cocina antigua completamente desmontada con el termo eléctrico a la vista',
        caption: 'La cocina original vaciada por completo. El termo eléctrico fue retirado y condenado: el agua caliente pasaría a producirse con placas solares térmicas.',
        badge: 'Demolición',
        orientation: 'portrait',
      },
    ],
  },
  {
    number: '03',
    kicker: 'Fase 03 · Obra civil e instalaciones',
    title: 'Condenar el pasado y cablear la nueva ubicación',
    body:
      'Aquí la casa cambió de forma. El antiguo acceso a la cocina se condenó con un tabique de bloque de 20 cm, enfrascado con mortero y acabado fino hasta borrar cualquier rastro de la vieja puerta doble. En paralelo, se abrieron las nuevas ranuras en paredes y suelos para llevar la electricidad y la fontanería hasta la nueva ubicación, con todo el cableado protegido en tubería corrugada flexible. Lo que no se ve —circuitos, tomas, llaves— quedó construido para durar.',
    checks: [
      'Tabique de bloque de 20 cm con enfrascado y acabado fino',
      'Ranuras en paredes y suelos para los nuevos recorridos',
      'Cableado protegido con tubería corrugada flexible',
      'Fontanería reubicada íntegramente a la nueva ubicación',
    ],
    images: [
      {
        src: '/images/proyectos/cocina-lujo-guataparo/obra-tabique-ranuras-electricas.webp',
        alt: 'Tabique de bloque condenando el acceso antiguo y ranuras eléctricas en las paredes',
        caption: 'El acceso original condenado con bloque de 20 cm; alrededor, las nuevas ranuras eléctricas recorren paredes y techo.',
        badge: 'Obra civil',
        orientation: 'landscape',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/obra-acceso-condenado-fratasado.webp',
        alt: 'Antiguo acceso ya enfrascado y la futura ubicación de la cocina en proceso',
        caption: 'Con el enfrascado de acabado fino, el antiguo hueco de la puerta doble desaparece de la pared para siempre.',
        badge: 'Enfrascado',
        orientation: 'landscape',
      },
    ],
  },
  {
    number: '04',
    kicker: 'Fase 04 · El suelo',
    title: 'Autonivelante, imprimación y un epoxi blanco sin juntas',
    body:
      'El encargo pedía algo más que un buen suelo: pedía un diseño. La respuesta fue un pavimento continuo que uniera cocina, comedor y sala sin una sola junta. Primero se vertió el autonivelante hasta lograr un plano milimetrado; después, la imprimación de anclaje; y por último, el acabado epóxico en blanco puro. El resultado es una superficie única que multiplica la luz natural y da a toda la planta esa sensación —difícil de falsificar— de continuidad perfecta.',
    checks: [
      'Plano nivelado al milímetro con mortero autonivelante',
      'Imprimación de anclaje en toda la superficie',
      'Acabado epóxico blanco de continuidad total, sin juntas',
    ],
    images: [
      {
        src: '/images/proyectos/cocina-lujo-guataparo/suelo-autonivelante-aplicado.webp',
        alt: 'Suelo autonivelante recién aplicado en la nueva ubicación de la cocina',
        caption: 'Primera capa: el autonivelante recién vertido deja un plano perfecto como base de todo el pavimento.',
        badge: 'Base',
        orientation: 'landscape',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/suelo-imprimacion-blanca.webp',
        alt: 'Suelo con imprimación blanca aplicada antes del acabado epóxico',
        caption: 'La imprimación de anclaje en blanco: la superficie queda preparada para recibir el acabado epóxico final.',
        badge: 'Imprimación',
        orientation: 'landscape',
      },
    ],
  },
  {
    number: '05',
    kicker: 'Fase 05 · Montaje',
    title: 'Solo muebles bajos y una estantería flotante con luz oculta',
    body:
      'Con la obra gruesa terminada llegó la parte que más se disfruta: el montaje. Se optó por una composición estrictamente de muebles bajos —sin módulos altos— para que la línea de la cocina quedara baja, limpia y minimalista. Por encima, una estantería flotante recorre toda la longitud con dos tiras LED, superior e inferior, invisibles a la vista: la luz baña pared y encimera sin que se adivine de dónde sale.',
    checks: [
      'Módulos bajos montados con control de plomos, niveles y encuentros',
      'Estantería flotante corrida a toda la longitud de la cocina',
      'Iluminación LED integrada, invisible desde cualquier ángulo',
    ],
    images: [
      {
        src: '/images/proyectos/cocina-lujo-guataparo/montaje-cocina-vista-sala.webp',
        alt: 'Cocina en proceso de montaje vista desde la sala con el suelo epóxico terminado',
        caption: 'El montaje visto desde la sala: con el suelo ya continuo, la nueva cocina empieza a conversar con el resto de la planta.',
        badge: 'Proceso',
        orientation: 'landscape',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/montaje-estante-flotante-lineal.webp',
        alt: 'Detalle del montaje de la cocina lineal con la estantería flotante en ajuste',
        caption: 'La estantería flotante corrida durante su ajuste, con la iluminación LED ya integrada en el canto.',
        badge: 'Estantería',
        orientation: 'portrait',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/montaje-estante-flotante-lateral.webp',
        alt: 'Vista lateral de la cocina lineal en montaje frente al ventanal',
        caption: 'Sin muebles altos: toda la composición queda bajo la línea de vista y deja respirar al ventanal.',
        badge: 'Proceso',
        orientation: 'portrait',
      },
    ],
  },
  {
    number: '06',
    kicker: 'Fase 06 · Entrega',
    title: 'Puesta en marcha: equipos Siemens y revisión final',
    body:
      'El cierre: horno y lavavajillas Siemens integrados, y una nevera importada de 90 cm de la misma casa. Se revisó cada luz, cada junta y cada canto, y la quinta recibió algo que nunca había tenido: una zona día de una sola pieza, donde la cocina por fin vive con la casa —y no detrás de una puerta—.',
    checks: [
      'Horno y lavavajillas Siemens integrados en la composición',
      'Nevera importada de 90 cm incorporada a la línea',
      'Revisión final de acabados, iluminación y funcionamiento',
    ],
    images: [
      {
        src: '/images/proyectos/cocina-lujo-guataparo/resultado-final-cocina-lineal.webp',
        alt: 'Cocina de lujo terminada en Guataparo con muebles bajos, estantería iluminada y equipos Siemens',
        caption: 'La cocina entregada: línea de muebles bajos en blanco, estantería iluminada y nevera Siemens de 90 cm.',
        badge: 'Entrega',
        orientation: 'landscape',
      },
      {
        src: '/images/proyectos/cocina-lujo-guataparo/resultado-final-comedor-abierto.webp',
        alt: 'Comedor y cocina abiertos con suelo epóxico blanco continuo',
        caption: 'La zona día unificada: el epoxi blanco continuo conecta cocina, comedor y sala en una sola lectura.',
        badge: 'Entrega',
        orientation: 'landscape',
      },
    ],
  },
];

const highlights = [
  {
    icon: 'layers',
    title: 'Suelo epóxico blanco continuo',
    text: 'Autonivelante, imprimación y acabado epóxico en blanco puro: una sola superficie sin juntas que unifica cocina, comedor y sala.',
  },
  {
    icon: 'light',
    title: 'Estantería flotante con luz oculta',
    text: 'Recorre toda la longitud de la cocina y esconde dos tiras LED —superior e inferior— que bañan pared y encimera sin verse.',
  },
  {
    icon: 'sun',
    title: 'Agua caliente solar',
    text: 'El termo eléctrico quedó condenado: el agua caliente se produce ahora con placas solares térmicas, más eficientes y sostenibles.',
  },
  {
    icon: 'appliance',
    title: 'Equipamiento Siemens',
    text: 'Horno y lavavajillas integrados, más una nevera importada de 90 cm: equipos de especificación alemana para uso diario intensivo.',
  },
];

const icons = {
  layers: '<svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg>',
  light: '<svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 18h6"/><path d="M10 22h4"/><path d="M12 2a7 7 0 0 0-4 12.7c.6.5 1 1.3 1 2.1V17h6v-.2c0-.8.4-1.6 1-2.1A7 7 0 0 0 12 2z"/></svg>',
  sun: '<svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"/></svg>',
  appliance: '<svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="2" width="18" height="20" rx="2"/><circle cx="12" cy="13" r="5"/><circle cx="12" cy="13" r="1.6"/><path d="M6 5.5h.01M9 5.5h6"/></svg>',
};

const specs = [
  { label: 'Ubicación', value: 'Quinta residencial en Guataparo, Valencia, Carabobo' },
  { label: 'Duración de obra', value: '2 semanas y media de ejecución' },
  { label: 'Intervención', value: 'Reubicación integral de la cocina a la zona día' },
  { label: 'Estado inicial', value: 'Cocina cerrada e independiente, con acceso por puerta doble' },
  { label: 'Obra civil', value: 'Condena del acceso con bloque de 20 cm y acabado fino' },
  { label: 'Instalación eléctrica', value: 'Nuevas ranuras en paredes y suelos, tubo corrugado flexible' },
  { label: 'Fontanería', value: 'Reubicada íntegramente a la nueva ubicación' },
  { label: 'Agua caliente', value: 'Termo eléctrico condenado; placas solares térmicas' },
  { label: 'Suelo', value: 'Autonivelante + imprimación + epoxi blanco continuo' },
  { label: 'Mobiliario', value: 'Composición lineal de muebles bajos, estilo minimalista' },
  { label: 'Estantería', value: 'Flotante a toda la longitud, LED superior e inferior oculto' },
  { label: 'Equipos', value: 'Horno y lavavajillas Siemens; nevera importada de 90 cm' },
];

const galleryPhases = [
  {
    id: 'resultado',
    label: 'Resultado final',
    description: 'La cocina entregada: línea continua de muebles bajos, estantería flotante iluminada y el suelo epóxico blanco uniendo toda la zona día.',
  },
  {
    id: 'montaje',
    label: 'Montaje',
    description: 'Alineación de módulos bajos, encuentros y la estantería flotante con su iluminación integrada durante el proceso.',
  },
  {
    id: 'suelo',
    label: 'El suelo',
    description: 'Autonivelante, imprimación y acabado epóxico: las tres capas que construyen la continuidad perfecta del pavimento.',
  },
  {
    id: 'obra',
    label: 'Obra civil e instalaciones',
    description: 'Desmontaje, condena del acceso con bloque de 20 cm y las nuevas instalaciones eléctricas y de fontanería.',
  },
  {
    id: 'antes',
    label: 'Estado previo',
    description: 'El punto de partida real: la cocina cerrada, su acceso doble y el comedor donde hoy se lee el nuevo espacio.',
  },
];

const gallery = [
  {
    src: '/images/proyectos/cocina-lujo-guataparo/resultado-final-cocina-lineal.webp',
    alt: 'Cocina lineal terminada en blanco con estantería flotante iluminada y nevera Siemens en Guataparo',
    title: 'Composición lineal terminada',
    caption: 'Muebles bajos en blanco, estantería flotante con luz oculta y la nevera Siemens de 90 cm cerrando la línea.',
    category: 'resultado',
    categoryLabel: 'Resultado Final',
    badge: 'Finalizado',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/resultado-final-comedor-abierto.webp',
    alt: 'Comedor y cocina abiertos con suelo epóxico blanco continuo en quinta de Guataparo',
    title: 'La zona día unificada',
    caption: 'El epoxi blanco continuo conecta cocina, comedor y sala en una única lectura llena de luz.',
    category: 'resultado',
    categoryLabel: 'Resultado Final',
    badge: 'Zona día',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/montaje-cocina-vista-sala.webp',
    alt: 'Cocina en proceso de montaje vista desde la sala con el suelo epóxico blanco terminado',
    title: 'El montaje visto desde la sala',
    caption: 'La nueva ubicación ya dialoga con el resto de la planta: suelo continuo y composición baja en instalación.',
    category: 'montaje',
    categoryLabel: 'Montaje',
    badge: 'Proceso',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/montaje-estante-flotante-lineal.webp',
    alt: 'Detalle del montaje de la cocina lineal con estantería flotante en proceso de ajuste',
    title: 'Estantería flotante en ajuste',
    caption: 'La estantería flotante corrida, con la iluminación LED ya integrada en su canto superior e inferior.',
    category: 'montaje',
    categoryLabel: 'Montaje',
    badge: 'Estantería',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/montaje-estante-flotante-lateral.webp',
    alt: 'Vista lateral de la cocina lineal en montaje con el ventanal al fondo en Guataparo',
    title: 'La línea baja frente al ventanal',
    caption: 'Sin muebles altos: toda la composición queda por debajo de la línea de vista y deja respirar al ventanal.',
    category: 'montaje',
    categoryLabel: 'Montaje',
    badge: 'Proceso',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/suelo-autonivelante-aplicado.webp',
    alt: 'Aplicación del suelo autonivelante en la nueva ubicación de la cocina',
    title: 'Autonivelante recién vertido',
    caption: 'La primera capa: un plano perfectamente nivelado como base de todo el pavimento continuo.',
    category: 'suelo',
    categoryLabel: 'El Suelo',
    badge: 'Base',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/suelo-imprimacion-blanca.webp',
    alt: 'Suelo con imprimación blanca aplicada antes del acabado epóxico final',
    title: 'Imprimación de anclaje',
    caption: 'La imprimación prepara la superficie para el acabado epóxico definitivo en blanco puro.',
    category: 'suelo',
    categoryLabel: 'El Suelo',
    badge: 'Imprimación',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/obra-tabique-ranuras-electricas.webp',
    alt: 'Tabique de bloque de 20 cm condenando el acceso antiguo y ranuras eléctricas en pared',
    title: 'Condena del acceso y nuevas ranuras',
    caption: 'El acceso original sellado con bloque de 20 cm, mientras las nuevas ranuras preparan los circuitos.',
    category: 'obra',
    categoryLabel: 'Obra Civil e Instalaciones',
    badge: 'Obra civil',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/obra-acceso-condenado-fratasado.webp',
    alt: 'Antiguo acceso ya enfrascado y la futura ubicación de la cocina en pleno proceso',
    title: 'El acceso desaparece en la pared',
    caption: 'Con el enfrascado de acabado fino, el antiguo hueco de la puerta doble deja de existir visualmente.',
    category: 'obra',
    categoryLabel: 'Obra Civil e Instalaciones',
    badge: 'Enfrascado',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/obra-desmontaje-termo-electrico.webp',
    alt: 'Cocina antigua desmontada con termo eléctrico visible antes de su condena',
    title: 'Desmontaje y retirada del termo',
    caption: 'La cocina original vaciada; el termo eléctrico fue condenado y sustituido por placas solares térmicas.',
    category: 'obra',
    categoryLabel: 'Obra Civil e Instalaciones',
    badge: 'Demolición',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-2.webp',
    alt: 'Cocina original cerrada vista desde su acceso doble antes de la reforma',
    title: 'El acceso doble original',
    caption: 'La puerta doble de gran tamaño que separaba la cocina de la vida de la casa.',
    category: 'antes',
    categoryLabel: 'Estado Previo',
    badge: 'Antes',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-4.webp',
    alt: 'Interior de la cocina original con ventana y mobiliario antiguo',
    title: 'La cocina tal como se encontraba',
    caption: 'Cerámica, cenefa y mobiliario envejecidos: un espacio que pedía una intervención de raíz.',
    category: 'antes',
    categoryLabel: 'Estado Previo',
    badge: 'Antes',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-3.webp',
    alt: 'Detalle de la cocina original con encimera y módulos antiguos',
    title: 'Mobiliario y encimera envejecidos',
    caption: 'El conjunto original, ya fuera de servicio, fue desmontado por completo.',
    category: 'antes',
    categoryLabel: 'Estado Previo',
    badge: 'Antes',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/antes-cocina-original-1.webp',
    alt: 'Vista interior de la cocina cerrada original desde la puerta doble',
    title: 'Un espacio cerrado y compartimentado',
    caption: 'La planta original impedía cualquier continuidad entre cocina, comedor y sala.',
    category: 'antes',
    categoryLabel: 'Estado Previo',
    badge: 'Antes',
    orientation: 'portrait',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/antes-acceso-doble-sala.webp',
    alt: 'Sala y comedor originales con el acceso doble a la cocina al fondo',
    title: 'La zona día antes de la intervención',
    caption: 'Comedor y sala con la cocina al fondo, separada por el acceso doble que después fue condenado.',
    category: 'antes',
    categoryLabel: 'Estado Previo',
    badge: 'Antes',
    orientation: 'landscape',
  },
  {
    src: '/images/proyectos/cocina-lujo-guataparo/antes-nueva-ubicacion-comedor.webp',
    alt: 'Comedor original que se convertiría en la nueva ubicación de la cocina',
    title: 'La futura ubicación de la cocina',
    caption: 'El comedor original: el espacio elegido para recibir la nueva cocina abierta.',
    category: 'antes',
    categoryLabel: 'Estado Previo',
    badge: 'Antes',
    orientation: 'portrait',
  },
];
---

<BaseLayout
  title="Cocina de Lujo en Guataparo, Valencia | Caso Real Paso a Paso | RemodelaT"
  description={project.excerpt}
  image={project.heroImage}
  activePage="proyectos"
>
  <!-- Barra de progreso de lectura -->
  <div class="gu-progress" aria-hidden="true" data-progress><span></span></div>

  <!-- ============ HERO CINEMATOGRÁFICO ============ -->
  <header class="gu-hero">
    <img
      src={project.heroImage}
      alt="Cocina de lujo terminada en quinta de Guataparo, Valencia: línea de muebles bajos blancos, estantería flotante iluminada y suelo epóxico continuo"
      class="gu-hero-img"
      fetchpriority="high"
      loading="eager"
      decoding="async"
    />
    <div class="gu-hero-overlay" aria-hidden="true"></div>
    <div class="container gu-hero-content">
      <a href="/proyectos" class="gu-back" data-hero="0">← Volver a proyectos</a>
      <span class="gu-eyebrow" data-hero="1">Caso real · Cocinas de alto estándar · Diario de obra</span>
      <h1 data-hero="2">{project.title}</h1>
      <p class="gu-lead" data-hero="3">{project.excerpt}</p>

      <dl class="gu-facts" aria-label="Ficha del proyecto" data-hero="4">
        {heroFacts.map((fact) => (
          <div class="gu-fact">
            <dt>{fact.label}</dt>
            <dd>{fact.value}</dd>
          </div>
        ))}
      </dl>

      <div class="gu-hero-actions" data-hero="5">
        <a href="#paso-a-paso" class="btn-premium btn-primary-premium">
          <span class="btn-content">Ver el paso a paso</span>
          <span class="btn-border"></span>
          <span class="btn-arrow">→</span>
        </a>
        <a href={whatsappHref} class="btn-premium btn-outline-premium" target="_blank" rel="noopener">
          <span class="btn-content">Solicitar valoración</span>
          <span class="btn-arrow">→</span>
        </a>
      </div>
    </div>
    <div class="gu-scrollcue" aria-hidden="true">
      <span class="gu-scrollcue-text">Desliza para leer la obra</span>
      <span class="gu-scrollcue-line"></span>
    </div>
  </header>

  <!-- ============ NAVEGACIÓN RÁPIDA STICKY ============ -->
  <ProjectQuickNav
    items={quickNavItems}
    ctaHref={whatsappHref}
    ctaLabel="Solicitar valoración"
    summary="Reubicación integral en 2 semanas y media, documentada fase a fase."
  />

  <!-- ============ MIGAS DE PAN ============ -->
  <nav class="gu-breadcrumb" aria-label="Breadcrumb">
    <div class="container gu-breadcrumb-inner">
      <a href="/">Inicio</a>
      <span class="gu-sep">/</span>
      <a href="/proyectos">Proyectos</a>
      <span class="gu-sep">/</span>
      <span class="gu-current">Cocina de Lujo en Guataparo</span>
    </div>
  </nav>

  <!-- ============ 01 · RESUMEN ============ -->
  <section class="section gu-summary" id="resumen">
    <div class="container">
      <div class="section-header" data-reveal>
        <span class="tag">En síntesis</span>
        <h2 class="section-title">De cocina cerrada a corazón abierto de la quinta</h2>
        <p class="section-desc">
          Una intervención que no se limitó a cambiar acabados: la cocina cambió de habitación,
          de concepto y de relación con el resto de la vivienda.
        </p>
      </div>

      <div class="gu-summary-grid">
        <article class="gu-summary-card" data-reveal>
          <span class="gu-summary-index">El reto</span>
          <h3>Mover una cocina entera sin dejar rastro de la antigua</h3>
          <p class="gu-dropcap">
            La cocina original era un compartimento cerrado, al que se entraba por una gran puerta
            doble. Trasladarla implicaba mucho más que montar muebles nuevos: había que condenar
            su acceso, reinventar la electricidad y la fontanería, retirar el termo eléctrico y
            crear un pavimento que unificara toda la zona día.
          </p>
        </article>
        <article class="gu-summary-card" data-reveal>
          <span class="gu-summary-index">La solución</span>
          <h3>Obra civil, instalaciones nuevas y un diseño blanco continuo</h3>
          <p>
            Condenamos el acceso con bloque de 20 cm y acabado fino, trazamos las nuevas
            instalaciones por paredes y suelos, sustituimos el termo por placas solares térmicas
            y vertimos un suelo epóxico blanco sin juntas. Sobre ese lienzo, una composición
            lineal de muebles bajos con estantería flotante iluminada y equipos Siemens.
          </p>
        </article>
      </div>

      <!-- Banda de cifras -->
      <dl class="gu-stats" aria-label="El proyecto en cifras" data-reveal>
        {stats.map((stat) => (
          <div class="gu-stat">
            <dt>{stat.label}</dt>
            <dd>{stat.value}</dd>
          </div>
        ))}
      </dl>
    </div>
  </section>

  <!-- ============ 02 · RESULTADO FINAL ============ -->
  <section class="section gu-showcase" id="resultado">
    <div class="container">
      <div class="section-header" data-reveal>
        <span class="tag">Resultado final</span>
        <h2 class="section-title">Primero, el destino; después, el camino</h2>
        <p class="section-desc">
          Antes de explicar cada fase, mostramos el resultado: así cada paso del proceso
          se entiende sabiendo a dónde conducía.
        </p>
      </div>

      <div class="gu-showcase-grid">
        {resultadoShowcase.map((item, index) => (
          <figure
            class="gu-showcase-card"
            data-reveal
            data-lightbox-item
            data-lightbox-group="guataparo-resultado"
            data-src={item.src}
            data-alt={item.alt}
            data-title={item.title}
            data-caption={item.caption}
            data-badge={item.badge}
          >
            <div class="gu-showcase-media">
              <img src={item.src} alt={item.alt} loading={index === 0 ? 'eager' : 'lazy'} decoding="async" />
              <span class="gu-media-chip" aria-hidden="true">Ampliar</span>
            </div>
            <figcaption>
              <span class="gu-showcase-badge">{item.badge}</span>
              <h3>{item.title}</h3>
              <p>{item.caption}</p>
              <button
                class="expand-photo-btn"
                type="button"
                data-lightbox-open
                data-group="guataparo-resultado"
                data-src={item.src}
              >Ver en grande</button>
            </figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ PULL QUOTE ============ -->
  <section class="gu-quote" aria-label="Cita del proyecto">
    <div class="container gu-quote-inner" data-reveal>
      <span class="gu-quote-mark" aria-hidden="true">“</span>
      <blockquote>
        Donde antes terminaba el comedor y empezaba una puerta doble,
        hoy hay una sola estancia llena de luz.
      </blockquote>
      <cite>Diario de obra · Quinta en Guataparo</cite>
    </div>
  </section>

  <!-- ============ 03 · PASO A PASO ============ -->
  <section class="section gu-timeline-section" id="paso-a-paso">
    <div class="container">
      <div class="section-header" data-reveal>
        <span class="tag">La obra, en orden</span>
        <h2 class="section-title">El proyecto paso a paso: seis fases, cero improvisación</h2>
        <p class="section-desc">
          Cada fase está numerada y documentada con sus trabajos ejecutados y <strong>todas</strong> sus
          fotografías reales, para que el recorrido se lea tan claro como se ejecutó.
        </p>
        <div class="gu-timeline-meta" aria-hidden="true">
          <span>06 fases</span>
          <span class="gu-timeline-meta-dot"></span>
          <span>16 fotografías</span>
          <span class="gu-timeline-meta-dot"></span>
          <span>2 semanas y media</span>
        </div>
      </div>

      <div class="gu-flow">
        <!-- Navegador lateral de fases -->
        <aside class="gu-flow-nav" aria-label="Índice de fases">
          <span class="gu-flow-nav-title">Fases</span>
          <ol>
            {phaseNav.map((item) => (
              <li>
                <a href={`#fase-${item.number}`} data-fase-link={item.number}>
                  <span class="gu-flow-nav-num">{item.number}</span>
                  <span class="gu-flow-nav-label">{item.label}</span>
                </a>
              </li>
            ))}
          </ol>
          <span class="gu-flow-nav-progress" aria-hidden="true"><span data-flow-progress></span></span>
        </aside>

        <!-- Fases -->
        <div class="gu-flow-main">
          {phases.map((phase, phaseIndex) => (
            <article class="gu-fase" id={`fase-${phase.number}`} data-fase={phase.number} data-reveal>
              <div class="gu-fase-head">
                <div class="gu-fase-numside" aria-hidden="true">
                  <span class="gu-fase-num">{phase.number}</span>
                  {phaseIndex < phases.length - 1 && <span class="gu-fase-numline"></span>}
                </div>
                <div class="gu-fase-copy">
                  <span class="gu-fase-kicker">{phase.kicker}</span>
                  <h3>{phase.title}</h3>
                  <p>{phase.body}</p>
                  <ul class="gu-fase-checks">
                    {phase.checks.map((check) => (
                      <li>{check}</li>
                    ))}
                  </ul>
                </div>
              </div>

              <div class:list={['gu-fg', `gu-fg--${phase.images.length}`]}>
                {phase.images.map((image, imageIndex) => (
                  <figure
                    class:list={['gu-fg-item', imageIndex === 0 && 'gu-fg-item--featured']}
                    data-lightbox-item
                    data-lightbox-group={`gu-fase-${phase.number}`}
                    data-src={image.src}
                    data-alt={image.alt}
                    data-title={`${phase.kicker} · ${image.badge}`}
                    data-caption={image.caption}
                    data-badge={phase.kicker}
                  >
                    <div class="gu-fg-media">
                      <img src={image.src} alt={image.alt} loading="lazy" decoding="async" />
                      <span class="gu-fg-badge">{image.badge}</span>
                      <span class="gu-media-chip" aria-hidden="true">Ampliar</span>
                    </div>
                    <figcaption>{image.caption}</figcaption>
                  </figure>
                ))}
              </div>
            </article>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ============ 04 · ANTES / DESPUÉS INTERACTIVO ============ -->
  <section class="gu-compare-section section" id="comparativa">
    <div class="container">
      <div class="section-header" data-reveal>
        <span class="tag">La misma zona, otra vida</span>
        <h2 class="section-title">Desliza y compruébalo con tus propios ojos</h2>
        <p class="section-desc">
          La comparación se toma sobre la zona día de la quinta: donde antes terminaba el comedor
          y empezaba el pasillo hacia la cocina cerrada, hoy se lee un único espacio continuo.
        </p>
      </div>

      <div class="gu-ba" data-ba data-reveal>
        <div class="gu-ba-stage" data-ba-stage role="slider" tabindex="0" aria-label="Comparador antes y después: arrastra para comparar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="50">
          <img
            class="gu-ba-img gu-ba-img--after"
            src="/images/proyectos/cocina-lujo-guataparo/resultado-final-comedor-abierto.webp"
            alt="Después: la misma zona día convertida en cocina abierta con suelo epóxico blanco continuo"
            loading="lazy"
            decoding="async"
            draggable="false"
          />
          <div class="gu-ba-before" data-ba-before>
            <img
              class="gu-ba-img"
              src="/images/proyectos/cocina-lujo-guataparo/antes-acceso-doble-sala.webp"
              alt="Antes: zona día original con el acceso doble a la cocina cerrada al fondo"
              loading="lazy"
              decoding="async"
              draggable="false"
            />
          </div>
          <span class="gu-ba-chip gu-ba-chip--before">Antes</span>
          <span class="gu-ba-chip gu-ba-chip--after">Después</span>
          <div class="gu-ba-handle" data-ba-handle aria-hidden="true">
            <span class="gu-ba-handle-line"></span>
            <span class="gu-ba-handle-knob">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><polyline points="8 7 3 12 8 17"/><polyline points="16 7 21 12 16 17"/></svg>
            </span>
            <span class="gu-ba-handle-line"></span>
          </div>
        </div>
        <div class="gu-ba-captions">
          <div>
            <strong>Antes</strong>
            <p>El comedor original, con la cocina encerrada detrás del acceso doble y un suelo cerámico que cortaba la lectura del espacio.</p>
          </div>
          <div>
            <strong>Después</strong>
            <p>La misma zona, unificada: cocina abierta de línea baja, estantería iluminada y pavimento epóxico blanco continuo.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ 05 · ESPECIFICACIONES ============ -->
  <section class="section gu-specs" id="especificaciones">
    <div class="container">
      <div class="section-header" data-reveal>
        <span class="tag">Detalle técnico</span>
        <h2 class="section-title">Especificaciones de la intervención</h2>
        <p class="section-desc">
          Lo que se ve —y lo que no se ve— de esta obra, documentado partida por partida.
        </p>
      </div>

      <div class="gu-highlights">
        {highlights.map((item, index) => (
          <article class="gu-highlight-card" data-reveal>
            <div class="gu-highlight-top">
              <span class="gu-highlight-icon" set:html={icons[item.icon]} />
              <span class="gu-highlight-index">{String(index + 1).padStart(2, '0')}</span>
            </div>
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </article>
        ))}
      </div>

      <div class="gu-spec-table" role="table" aria-label="Ficha técnica del proyecto" data-reveal>
        {specs.map((spec) => (
          <div class="gu-spec-row" role="row">
            <strong role="cell">{spec.label}</strong>
            <span role="cell">{spec.value}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ 06 · GALERÍA COMPLETA ============ -->
  <ProjectPhaseGallery
    eyebrow="Archivo fotográfico completo"
    title={`Galería completa por fases (${gallery.length} fotos reales)`}
    intro="Todas las imágenes del proyecto, organizadas en el mismo orden en que se ejecutó la obra: del resultado al origen, sin mezclas ni rellenos."
    items={gallery}
    phases={galleryPhases}
    id="galeria"
  />

  <!-- ============ GARANTÍAS ============ -->
  <GuaranteeMatrix compact />

  <!-- ============ CTA FINAL ============ -->
  <section class="gu-cta">
    <div class="container gu-cta-inner" data-reveal>
      <span class="tag tag-on-dark">Valoración técnica</span>
      <h2>¿Imaginas una transformación así en tu cocina?</h2>
      <p>
        Reubicaciones, obra civil, instalaciones, pavimentos continuos y mobiliario de alta
        especificación. Cuéntanos tu caso: valoramos el alcance técnico y te orientamos
        sobre tiempos, materiales y nivel de acabado.
      </p>
      <div class="gu-cta-actions">
        <a href={whatsappHref} class="btn-premium btn-primary-premium" target="_blank" rel="noopener">
          <span class="btn-content">Solicitar valoración técnica</span>
          <span class="btn-border"></span>
          <span class="btn-arrow">→</span>
        </a>
        <a href="/proyectos" class="gu-cta-secondary">Ver más proyectos →</a>
      </div>
    </div>
  </section>

  <ProjectLightbox />
</BaseLayout>

<script>
  (function () {
    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    /* ---------- Barra de progreso de lectura ---------- */
    const progressBar = document.querySelector('[data-progress] span');
    if (progressBar) {
      const updateProgress = () => {
        const doc = document.documentElement;
        const max = doc.scrollHeight - doc.clientHeight;
        const ratio = max > 0 ? Math.min(1, window.scrollY / max) : 0;
        progressBar.style.transform = `scaleX(${ratio})`;
      };
      updateProgress();
      window.addEventListener('scroll', updateProgress, { passive: true });
      window.addEventListener('resize', updateProgress, { passive: true });
    }

    /* ---------- Revelado al hacer scroll ---------- */
    const revealTargets = document.querySelectorAll('[data-reveal]');
    if (revealTargets.length && 'IntersectionObserver' in window && !prefersReduced) {
      revealTargets.forEach((el) => el.classList.add('reveal'));
      const revealObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-in');
              revealObserver.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.12, rootMargin: '0px 0px -6% 0px' }
      );
      revealTargets.forEach((el) => revealObserver.observe(el));
    }

    /* ---------- Navegador de fases (sticky) ---------- */
    const faseSections = document.querySelectorAll('[data-fase]');
    const faseLinks = document.querySelectorAll('[data-fase-link]');
    const flowProgress = document.querySelector('[data-flow-progress]');
    if (faseSections.length && faseLinks.length && 'IntersectionObserver' in window) {
      const setActive = (num) => {
        faseLinks.forEach((link) => {
          const active = link.getAttribute('data-fase-link') === num;
          link.classList.toggle('is-active', active);
          if (active) link.setAttribute('aria-current', 'true');
          else link.removeAttribute('aria-current');
        });
        if (flowProgress) {
          const index = Math.max(0, Array.from(faseSections).findIndex((s) => s.getAttribute('data-fase') === num));
          flowProgress.style.transform = `scaleY(${(index + 1) / faseSections.length})`;
        }
      };
      const phaseObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) setActive(entry.target.getAttribute('data-fase'));
          });
        },
        { rootMargin: '-38% 0px -52% 0px', threshold: 0 }
      );
      faseSections.forEach((section) => phaseObserver.observe(section));
      setActive('01');
    }

    /* ---------- Comparador antes / después ---------- */
    const ba = document.querySelector('[data-ba]');
    if (ba) {
      const stage = ba.querySelector('[data-ba-stage]');
      const before = ba.querySelector('[data-ba-before]');
      const handle = ba.querySelector('[data-ba-handle]');
      let pos = 50;
      let dragging = false;

      const render = () => {
        const clamped = Math.max(2, Math.min(98, pos));
        before.style.clipPath = `inset(0 ${100 - clamped}% 0 0)`;
        handle.style.left = `${clamped}%`;
        stage.setAttribute('aria-valuenow', String(Math.round(clamped)));
      };
      const fromEvent = (clientX) => {
        const rect = stage.getBoundingClientRect();
        pos = ((clientX - rect.left) / rect.width) * 100;
        render();
      };

      render();

      stage.addEventListener('pointerdown', (event) => {
        dragging = true;
        stage.setPointerCapture(event.pointerId);
        fromEvent(event.clientX);
      });
      stage.addEventListener('pointermove', (event) => {
        if (dragging) fromEvent(event.clientX);
      });
      ['pointerup', 'pointercancel'].forEach((type) =>
        stage.addEventListener(type, () => { dragging = false; })
      );
      stage.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') {
          event.preventDefault();
          pos += event.key === 'ArrowRight' ? 3 : -3;
          render();
        }
      });
    }
  })();
</script>

<style>
  /* ==========================================================
     BARRA DE PROGRESO DE LECTURA
     ========================================================== */
  .gu-progress {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    z-index: 130;
    background: transparent;
    pointer-events: none;
  }
  .gu-progress span {
    display: block;
    height: 100%;
    background: linear-gradient(90deg, var(--gold-ink), var(--gold), var(--gold-light));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.08s linear;
  }

  /* ==========================================================
     HERO CINEMATOGRÁFICO
     ========================================================== */
  .gu-hero {
    position: relative;
    min-height: 92vh;
    display: flex;
    align-items: flex-end;
    background: var(--ink);
    color: var(--white);
    overflow: hidden;
    padding: 150px 0 96px;
  }
  .gu-hero-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 62%;
    opacity: 0.72;
    animation: gu-kenburns 7s var(--ease-out) both;
  }
  @keyframes gu-kenburns {
    from { transform: scale(1.09); opacity: 0.55; }
    to { transform: scale(1); opacity: 0.72; }
  }
  .gu-hero-overlay {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(to top, rgba(20, 18, 14, 0.97) 0%, rgba(20, 18, 14, 0.55) 45%, rgba(20, 18, 14, 0.25) 75%),
      linear-gradient(100deg, rgba(20, 18, 14, 0.85) 0%, transparent 60%);
  }
  .gu-hero-content {
    position: relative;
    z-index: 2;
    max-width: 1080px;
  }
  [data-hero] {
    opacity: 0;
    transform: translateY(26px);
    animation: gu-hero-in 0.9s var(--ease-out) forwards;
  }
  [data-hero="0"] { animation-delay: 0.05s; }
  [data-hero="1"] { animation-delay: 0.18s; }
  [data-hero="2"] { animation-delay: 0.32s; }
  [data-hero="3"] { animation-delay: 0.46s; }
  [data-hero="4"] { animation-delay: 0.6s; }
  [data-hero="5"] { animation-delay: 0.74s; }
  @keyframes gu-hero-in {
    to { opacity: 1; transform: translateY(0); }
  }
  .gu-back {
    display: inline-block;
    color: var(--gold);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 26px;
    transition: color var(--dur-fast) var(--ease-out);
  }
  .gu-back:hover { color: var(--gold-light); }
  .gu-eyebrow {
    display: block;
    color: var(--gold-light);
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    margin-bottom: 20px;
  }
  .gu-hero h1 {
    font-family: var(--font-serif);
    font-size: clamp(3rem, 7.4vw, 6.4rem);
    font-weight: 500;
    line-height: 0.97;
    letter-spacing: var(--track-tight);
    max-width: 14ch;
    margin-bottom: 24px;
  }
  .gu-lead {
    max-width: 800px;
    color: rgba(255, 255, 255, 0.84);
    font-size: clamp(1.03rem, 1.6vw, 1.19rem);
    line-height: 1.78;
    margin-bottom: 40px;
  }
  .gu-facts {
    display: flex;
    flex-wrap: wrap;
    margin: 0 0 38px;
    border: 1px solid rgba(201, 169, 97, 0.3);
    border-radius: var(--r-lg);
    overflow: hidden;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(14px);
    max-width: 1090px;
  }
  .gu-fact {
    flex: 1 1 160px;
    padding: 18px 22px;
    border-right: 1px solid rgba(201, 169, 97, 0.18);
  }
  .gu-fact:last-child { border-right: 0; }
  .gu-fact dt {
    color: var(--gold);
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin-bottom: 6px;
  }
  .gu-fact dd {
    margin: 0;
    color: rgba(255, 255, 255, 0.92);
    font-size: 0.94rem;
    font-weight: 600;
    line-height: 1.4;
  }
  .gu-hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
  }
  .gu-hero-actions .btn-premium { min-height: 54px; }
  .gu-scrollcue {
    position: absolute;
    right: 34px;
    bottom: 40px;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }
  .gu-scrollcue-text {
    writing-mode: vertical-rl;
    color: rgba(255, 255, 255, 0.65);
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.22em;
    text-transform: uppercase;
  }
  .gu-scrollcue-line {
    width: 1px;
    height: 64px;
    background: linear-gradient(to bottom, var(--gold), transparent);
    position: relative;
    overflow: hidden;
  }
  .gu-scrollcue-line::after {
    content: '';
    position: absolute;
    top: -40%;
    left: 0;
    width: 100%;
    height: 40%;
    background: var(--white);
    animation: gu-scrollcue 2.2s var(--ease-out) infinite;
  }
  @keyframes gu-scrollcue {
    0% { top: -40%; }
    100% { top: 110%; }
  }

  /* ==========================================================
     MIGAS DE PAN
     ========================================================== */
  .gu-breadcrumb {
    background: var(--white);
    border-bottom: 1px solid var(--sand);
    padding: 16px 0;
  }
  .gu-breadcrumb-inner {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 8px;
    font-size: 0.84rem;
  }
  .gu-breadcrumb-inner a {
    color: var(--text-muted);
    font-weight: 500;
    text-decoration: none;
    transition: color var(--dur-fast) var(--ease-out);
  }
  .gu-breadcrumb-inner a:hover { color: var(--gold-ink); }
  .gu-sep { color: var(--sand); margin: 0 2px; }
  .gu-current { color: var(--text); font-weight: 600; }

  /* ==========================================================
     REVELADO AL SCROLL (aplicado por JS)
     ========================================================== */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.9s var(--ease-out), transform 0.9s var(--ease-out);
    will-change: opacity, transform;
  }
  .reveal.is-in {
    opacity: 1;
    transform: translateY(0);
  }

  /* ==========================================================
     RESUMEN + CIFRAS
     ========================================================== */
  .gu-summary { background: var(--white); scroll-margin-top: 118px; }
  .gu-summary .section-header { max-width: 760px; }
  .gu-summary-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 24px;
    max-width: 1080px;
    margin: 0 auto;
  }
  .gu-summary-card {
    position: relative;
    padding: 46px 40px 42px;
    border-radius: var(--r-lg);
    background: linear-gradient(180deg, var(--white), var(--bone));
    border: 1px solid var(--sand);
    box-shadow: var(--e-1);
    overflow: hidden;
    transition: transform var(--dur-base) var(--ease-out), box-shadow var(--dur-base) var(--ease-out);
  }
  .gu-summary-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--e-2);
  }
  .gu-summary-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 40px;
    right: 40px;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(201, 169, 97, 0.55), transparent);
  }
  .gu-summary-index {
    display: inline-block;
    color: var(--gold-ink-strong);
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin-bottom: 16px;
  }
  .gu-summary-card h3 {
    font-family: var(--font-serif);
    font-size: clamp(1.6rem, 2.6vw, 2.05rem);
    font-weight: 500;
    line-height: 1.14;
    color: var(--ink);
    margin-bottom: 18px;
  }
  .gu-summary-card p {
    margin: 0;
    color: var(--text-muted);
    font-size: 1.01rem;
    line-height: 1.82;
  }
  /* capitular editorial */
  .gu-dropcap::first-letter {
    font-family: var(--font-serif);
    font-size: 3.6em;
    font-weight: 600;
    line-height: 0.82;
    float: left;
    padding: 0.06em 0.14em 0 0;
    color: var(--gold-ink);
  }
  .gu-stats {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 0;
    max-width: 1080px;
    margin: 56px auto 0;
    border: 1px solid rgba(201, 169, 97, 0.25);
    border-radius: var(--r-lg);
    overflow: hidden;
    background: var(--ink);
    box-shadow: var(--e-2);
  }
  .gu-stat {
    padding: 30px 18px 28px;
    text-align: center;
    border-right: 1px solid rgba(201, 169, 97, 0.18);
  }
  .gu-stat:last-child { border-right: 0; }
  .gu-stat dd {
    margin: 0 0 8px;
    font-family: var(--font-serif);
    font-size: clamp(2.2rem, 4vw, 3.1rem);
    font-weight: 500;
    line-height: 1;
    color: var(--gold);
  }
  .gu-stat dt {
    color: rgba(255, 255, 255, 0.68);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    line-height: 1.5;
  }

  /* ==========================================================
     SHOWCASE RESULTADO
     ========================================================== */
  .gu-showcase {
    background: linear-gradient(180deg, var(--bone), var(--white));
    border-top: 1px solid var(--sand);
    scroll-margin-top: 118px;
  }
  .gu-showcase .section-header { max-width: 760px; }
  .gu-showcase-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 26px;
    max-width: 1160px;
    margin: 0 auto;
  }
  .gu-showcase-card {
    margin: 0;
    display: flex;
    flex-direction: column;
    border-radius: 26px;
    overflow: hidden;
    background: var(--white);
    border: 1px solid rgba(201, 169, 97, 0.16);
    box-shadow: var(--e-2);
    cursor: zoom-in;
    transition: transform var(--dur-base) var(--ease-out), box-shadow var(--dur-base) var(--ease-out), border-color var(--dur-base) var(--ease-out);
  }
  .gu-showcase-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--e-3);
    border-color: rgba(201, 169, 97, 0.4);
  }
  .gu-showcase-media {
    position: relative;
    padding: 18px;
    background:
      radial-gradient(circle at top, rgba(201, 169, 97, 0.14), transparent 42%),
      var(--ink-soft);
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 400px;
  }
  .gu-showcase-media img {
    max-width: 100%;
    max-height: 500px;
    width: auto;
    height: auto;
    object-fit: contain;
    border-radius: 8px;
    box-shadow: 0 20px 44px rgba(0, 0, 0, 0.32);
    transition: transform var(--dur-slow) var(--ease-out);
  }
  .gu-showcase-card:hover .gu-showcase-media img { transform: scale(1.02); }
  .gu-media-chip {
    position: absolute;
    right: 16px;
    bottom: 16px;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    min-height: 34px;
    padding: 0 13px;
    border-radius: var(--r-full);
    background: rgba(20, 18, 14, 0.82);
    border: 1px solid rgba(201, 169, 97, 0.3);
    color: rgba(255, 255, 255, 0.92);
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    backdrop-filter: blur(10px);
    pointer-events: none;
  }
  .gu-showcase-card figcaption {
    padding: 28px 30px 32px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }
  .gu-showcase-badge {
    display: inline-flex;
    align-items: center;
    padding: 5px 13px;
    border-radius: var(--r-full);
    background: var(--gold);
    color: var(--ink);
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 14px;
  }
  .gu-showcase-card figcaption h3 {
    font-family: var(--font-serif);
    font-size: clamp(1.55rem, 2.4vw, 2rem);
    font-weight: 500;
    line-height: 1.12;
    color: var(--ink);
    margin-bottom: 12px;
  }
  .gu-showcase-card figcaption p {
    margin: 0 0 18px;
    color: var(--text-muted);
    font-size: 0.98rem;
    line-height: 1.76;
  }

  /* ==========================================================
     PULL QUOTE
     ========================================================== */
  .gu-quote {
    padding: 96px 24px;
    background:
      radial-gradient(circle at 50% 110%, rgba(201, 169, 97, 0.16), transparent 55%),
      var(--ink);
    color: var(--white);
    border-top: 1px solid rgba(201, 169, 97, 0.18);
    border-bottom: 1px solid rgba(201, 169, 97, 0.18);
  }
  .gu-quote-inner {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }
  .gu-quote-mark {
    display: block;
    font-family: var(--font-serif);
    font-size: 7rem;
    line-height: 0.6;
    color: var(--gold);
    margin-bottom: 6px;
  }
  .gu-quote blockquote {
    margin: 0 0 26px;
    font-family: var(--font-serif);
    font-size: clamp(1.7rem, 3.6vw, 2.9rem);
    font-weight: 500;
    line-height: 1.22;
    letter-spacing: var(--track-tight);
    font-style: italic;
  }
  .gu-quote cite {
    font-style: normal;
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  /* ==========================================================
     PASO A PASO — flujo editorial con navegador lateral
     ========================================================== */
  .gu-timeline-section {
    background: var(--white);
    border-top: 1px solid var(--sand);
    scroll-margin-top: 118px;
  }
  .gu-timeline-section .section-header { max-width: 800px; }
  .gu-timeline-meta {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 26px;
    color: var(--gold-ink-strong);
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }
  .gu-timeline-meta-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--gold);
  }
  .gu-flow {
    display: grid;
    grid-template-columns: 200px minmax(0, 1fr);
    gap: 48px;
    max-width: 1180px;
    margin: 0 auto;
    align-items: start;
  }
  /* --- navegador lateral --- */
  .gu-flow-nav {
    position: sticky;
    top: 200px;
    padding: 26px 22px;
    border-radius: var(--r-lg);
    background: linear-gradient(180deg, var(--white), var(--bone));
    border: 1px solid var(--sand);
    box-shadow: var(--e-1);
  }
  .gu-flow-nav-title {
    display: block;
    color: var(--gold-ink-strong);
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    margin-bottom: 16px;
    padding-left: 6px;
  }
  .gu-flow-nav ol {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 4px;
  }
  .gu-flow-nav a {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 9px 8px;
    border-radius: var(--r-sm);
    color: var(--text-muted);
    text-decoration: none;
    transition: background var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out);
  }
  .gu-flow-nav a:hover { background: rgba(201, 169, 97, 0.1); color: var(--ink); }
  .gu-flow-nav-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    border: 1px solid rgba(201, 169, 97, 0.35);
    color: var(--gold-ink-strong);
    font-family: var(--font-serif);
    font-size: 1rem;
    font-weight: 600;
    flex-shrink: 0;
    transition: background var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out);
  }
  .gu-flow-nav-label {
    font-size: 0.86rem;
    font-weight: 600;
  }
  .gu-flow-nav a.is-active {
    background: var(--ink);
    color: var(--white);
  }
  .gu-flow-nav a.is-active .gu-flow-nav-num {
    background: var(--gold);
    border-color: var(--gold);
    color: var(--ink);
  }
  .gu-flow-nav-progress {
    display: block;
    height: 3px;
    margin-top: 18px;
    border-radius: 2px;
    background: var(--sand);
    overflow: hidden;
  }
  .gu-flow-nav-progress span {
    display: block;
    height: 100%;
    background: linear-gradient(90deg, var(--gold-ink), var(--gold));
    transform: scaleY(0.16);
    transform-origin: top;
    transition: transform 0.4s var(--ease-out);
  }
  /* --- fase --- */
  .gu-flow-main { min-width: 0; }
  .gu-fase {
    scroll-margin-top: 170px;
    padding: 46px 0 52px;
    border-top: 1px solid var(--sand);
  }
  .gu-fase:first-child { border-top: 0; padding-top: 8px; }
  .gu-fase-head {
    display: grid;
    grid-template-columns: 132px minmax(0, 1fr);
    gap: 30px;
    margin-bottom: 30px;
  }
  .gu-fase-numside {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }
  .gu-fase-num {
    font-family: var(--font-serif);
    font-size: clamp(3.6rem, 6vw, 5.2rem);
    font-weight: 500;
    line-height: 0.9;
    color: transparent;
    -webkit-text-stroke: 1.2px rgba(201, 169, 97, 0.8);
  }
  @supports not (-webkit-text-stroke: 1px black) {
    .gu-fase-num { color: rgba(201, 169, 97, 0.55); }
  }
  .gu-fase-numline {
    flex: 1;
    width: 1px;
    background: linear-gradient(to bottom, rgba(201, 169, 97, 0.55), transparent);
  }
  .gu-fase-copy { min-width: 0; max-width: 780px; }
  .gu-fase-kicker {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    margin-bottom: 15px;
    padding: 7px 15px;
    border-radius: var(--r-full);
    background: rgba(201, 169, 97, 0.13);
    color: var(--gold-ink-strong);
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .gu-fase-copy h3 {
    font-family: var(--font-serif);
    font-size: clamp(1.75rem, 3vw, 2.4rem);
    font-weight: 500;
    line-height: 1.08;
    letter-spacing: var(--track-tight);
    color: var(--ink);
    margin-bottom: 16px;
  }
  .gu-fase-copy > p {
    color: var(--text-muted);
    font-size: 1.03rem;
    line-height: 1.84;
    margin-bottom: 22px;
  }
  .gu-fase-checks {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px 22px;
  }
  .gu-fase-checks li {
    position: relative;
    padding-left: 30px;
    color: var(--text);
    font-size: 0.92rem;
    line-height: 1.55;
    font-weight: 500;
  }
  .gu-fase-checks li::before {
    content: '✓';
    position: absolute;
    left: 0;
    top: 1px;
    width: 19px;
    height: 19px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: rgba(201, 169, 97, 0.16);
    color: var(--gold-ink-strong);
    font-size: 0.68rem;
    font-weight: 800;
  }

  /* --- galería de fotos por fase (photo essay) --- */
  .gu-fg {
    display: grid;
    gap: 20px;
    margin-left: 0;
    padding-left: 162px;
  }
  .gu-fg-item {
    margin: 0;
    min-width: 0;
    cursor: zoom-in;
  }
  .gu-fg-media {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 300px;
    padding: 16px;
    border-radius: 20px;
    background:
      radial-gradient(circle at top, rgba(201, 169, 97, 0.13), transparent 40%),
      var(--ink-soft);
    overflow: hidden;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.03);
    transition: transform var(--dur-base) var(--ease-out), box-shadow var(--dur-base) var(--ease-out);
  }
  .gu-fg-item:hover .gu-fg-media {
    transform: translateY(-4px);
    box-shadow: var(--e-3);
  }
  .gu-fg-media img {
    max-width: 100%;
    max-height: 400px;
    width: auto;
    height: auto;
    object-fit: contain;
    border-radius: 6px;
    box-shadow: 0 16px 36px rgba(0, 0, 0, 0.3);
  }
  .gu-fg-badge {
    position: absolute;
    top: 14px;
    left: 14px;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    min-height: 30px;
    padding: 0 12px;
    border-radius: var(--r-full);
    background: var(--gold);
    color: var(--ink);
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .gu-fg-item figcaption {
    padding: 13px 8px 0;
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.65;
  }
  /* 1 foto: protagonista centrada */
  .gu-fg--1 {
    grid-template-columns: minmax(0, 1fr);
    max-width: 620px;
  }
  .gu-fg--1 .gu-fg-media { min-height: 460px; }
  .gu-fg--1 .gu-fg-media img { max-height: 560px; }
  /* 2 fotos: dupla equilibrada */
  .gu-fg--2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  /* 3 fotos: una protagonista + pareja */
  .gu-fg--3 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .gu-fg--3 .gu-fg-item--featured { grid-column: 1 / -1; }
  .gu-fg--3 .gu-fg-item--featured .gu-fg-media { min-height: 380px; }
  .gu-fg--3 .gu-fg-item--featured .gu-fg-media img { max-height: 460px; }
  /* 6 fotos: ensayo fotográfico 1 + 3 + 2 */
  .gu-fg--6 { grid-template-columns: repeat(6, minmax(0, 1fr)); }
  .gu-fg--6 .gu-fg-item { grid-column: span 2; }
  .gu-fg--6 .gu-fg-item--featured { grid-column: 1 / -1; }
  .gu-fg--6 .gu-fg-item--featured .gu-fg-media { min-height: 400px; }
  .gu-fg--6 .gu-fg-item--featured .gu-fg-media img { max-height: 480px; }
  .gu-fg--6 .gu-fg-item:nth-child(4) { grid-column: span 2; }
  .gu-fg--6 .gu-fg-item:nth-child(5) { grid-column: span 2; }
  .gu-fg--6 .gu-fg-item:nth-child(6) { grid-column: span 2; }

  /* ==========================================================
     COMPARADOR ANTES / DESPUÉS (drag)
     ========================================================== */
  .gu-compare-section {
    background: linear-gradient(180deg, var(--bone), var(--white));
    border-top: 1px solid var(--sand);
    scroll-margin-top: 118px;
  }
  .gu-compare-section .section-header { max-width: 780px; }
  .gu-ba {
    max-width: 1080px;
    margin: 0 auto;
  }
  .gu-ba-stage {
    position: relative;
    aspect-ratio: 4 / 3;
    border-radius: 26px;
    overflow: hidden;
    box-shadow: var(--e-3);
    border: 1px solid rgba(201, 169, 97, 0.25);
    cursor: ew-resize;
    touch-action: pan-y;
    user-select: none;
    background: var(--ink-soft);
  }
  .gu-ba-stage:focus-visible {
    outline: 3px solid var(--gold);
    outline-offset: 3px;
  }
  .gu-ba-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    pointer-events: none;
  }
  .gu-ba-before {
    position: absolute;
    inset: 0;
    clip-path: inset(0 50% 0 0);
  }
  .gu-ba-chip {
    position: absolute;
    top: 18px;
    z-index: 3;
    display: inline-flex;
    align-items: center;
    min-height: 36px;
    padding: 0 16px;
    border-radius: var(--r-full);
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    pointer-events: none;
  }
  .gu-ba-chip--before {
    left: 18px;
    background: rgba(20, 18, 14, 0.85);
    color: var(--white);
    border: 1px solid rgba(255, 255, 255, 0.14);
  }
  .gu-ba-chip--after {
    right: 18px;
    background: var(--gold);
    color: var(--ink);
  }
  .gu-ba-handle {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 50%;
    z-index: 4;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transform: translateX(-50%);
    pointer-events: none;
  }
  .gu-ba-handle-line {
    flex: 1;
    width: 2px;
    background: linear-gradient(to bottom, transparent, var(--white) 18%, var(--white) 82%, transparent);
    box-shadow: 0 0 14px rgba(0, 0, 0, 0.4);
  }
  .gu-ba-handle-knob {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 58px;
    height: 58px;
    border-radius: 50%;
    background: var(--gold);
    color: var(--ink);
    box-shadow: 0 10px 26px rgba(20, 18, 14, 0.4), 0 0 0 5px rgba(255, 255, 255, 0.28);
    flex-shrink: 0;
  }
  .gu-ba-captions {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 26px;
    margin-top: 26px;
  }
  .gu-ba-captions strong {
    display: block;
    font-family: var(--font-serif);
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 8px;
  }
  .gu-ba-captions p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.7;
  }

  /* ==========================================================
     ESPECIFICACIONES
     ========================================================== */
  .gu-specs {
    background: var(--white);
    border-top: 1px solid var(--sand);
    scroll-margin-top: 118px;
  }
  .gu-specs .section-header { max-width: 760px; }
  .gu-highlights {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 18px;
    max-width: 1160px;
    margin: 0 auto 34px;
  }
  .gu-highlight-card {
    padding: 28px 24px 26px;
    border-radius: var(--r-lg);
    background: linear-gradient(180deg, var(--white), var(--bone));
    border: 1px solid rgba(201, 169, 97, 0.18);
    box-shadow: var(--e-1);
    transition: transform var(--dur-base) var(--ease-out), box-shadow var(--dur-base) var(--ease-out), border-color var(--dur-base) var(--ease-out);
  }
  .gu-highlight-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--e-2);
    border-color: rgba(201, 169, 97, 0.4);
  }
  .gu-highlight-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 18px;
  }
  .gu-highlight-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: var(--ink);
    color: var(--gold);
  }
  .gu-highlight-index {
    font-family: var(--font-serif);
    font-size: 1.7rem;
    line-height: 1;
    color: rgba(201, 169, 97, 0.6);
  }
  .gu-highlight-card h3 {
    font-family: var(--font-serif);
    font-size: 1.32rem;
    font-weight: 600;
    line-height: 1.16;
    color: var(--ink);
    margin-bottom: 10px;
  }
  .gu-highlight-card p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.68;
  }
  .gu-spec-table {
    max-width: 980px;
    margin: 0 auto;
    border: 1px solid var(--sand);
    border-radius: var(--r-lg);
    overflow: hidden;
    box-shadow: var(--e-1);
  }
  .gu-spec-row {
    display: grid;
    grid-template-columns: minmax(200px, 0.62fr) 1fr;
    border-top: 1px solid var(--sand);
    background: var(--white);
  }
  .gu-spec-row:first-child { border-top: 0; }
  .gu-spec-row:nth-child(even) { background: var(--bone); }
  .gu-spec-row strong,
  .gu-spec-row span { padding: 16px 22px; }
  .gu-spec-row strong {
    color: var(--ink);
    font-family: var(--font-serif);
    font-size: 1.1rem;
    font-weight: 600;
    border-right: 1px solid var(--sand);
  }
  .gu-spec-row span {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  /* ==========================================================
     CTA FINAL
     ========================================================== */
  .gu-cta {
    padding: 110px 24px;
    background:
      radial-gradient(circle at 50% 0%, rgba(201, 169, 97, 0.2), transparent 48%),
      var(--ink);
    color: var(--white);
    text-align: center;
  }
  .gu-cta-inner { max-width: 880px; margin: 0 auto; }
  .gu-cta h2 {
    font-family: var(--font-serif);
    font-size: clamp(2.2rem, 4.8vw, 3.7rem);
    font-weight: 500;
    line-height: 1.06;
    letter-spacing: var(--track-tight);
    margin-bottom: 20px;
  }
  .gu-cta p {
    max-width: 720px;
    margin: 0 auto 36px;
    color: rgba(255, 255, 255, 0.75);
    font-size: 1.08rem;
    line-height: 1.78;
  }
  .gu-cta-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 22px;
  }
  .gu-cta-secondary {
    color: var(--gold);
    font-size: 0.88rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
    border-bottom: 1px solid rgba(201, 169, 97, 0.4);
    padding-bottom: 4px;
    transition: color var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out);
  }
  .gu-cta-secondary:hover { color: var(--gold-light); border-color: var(--gold-light); }
  .tag-on-dark { color: var(--gold); }

  #galeria { scroll-margin-top: 118px; }

  /* ==========================================================
     MOVIMIENTO REDUCIDO
     ========================================================== */
  @media (prefers-reduced-motion: reduce) {
    .gu-hero-img,
    .gu-scrollcue-line::after { animation: none; }
    [data-hero] { opacity: 1; transform: none; animation: none; }
  }

  /* ==========================================================
     RESPONSIVE
     ========================================================== */
  @media (max-width: 1100px) {
    .gu-flow { grid-template-columns: 1fr; gap: 0; }
    .gu-flow-nav { display: none; }
    .gu-fg { padding-left: 0; margin-top: 6px; }
    .gu-stats { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .gu-stat { border-bottom: 1px solid rgba(201, 169, 97, 0.18); }
  }

  @media (max-width: 820px) {
    .gu-hero { padding: 135px 0 80px; min-height: 0; }
    .gu-facts { border-radius: var(--r-md); }
    .gu-fact { flex: 1 1 46%; border-bottom: 1px solid rgba(201, 169, 97, 0.18); }
    .gu-scrollcue { display: none; }
    .gu-summary-grid { grid-template-columns: 1fr; }
    .gu-showcase-grid { grid-template-columns: 1fr; }
    .gu-highlights { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .gu-fase-checks { grid-template-columns: 1fr; }
    .gu-fase-head { grid-template-columns: 74px minmax(0, 1fr); gap: 20px; }
    .gu-fase-num { font-size: 3rem; }
    .gu-ba-captions { grid-template-columns: 1fr; }
  }

  @media (max-width: 640px) {
    .gu-hero h1 { font-size: clamp(2.4rem, 10vw, 3.1rem); }
    .gu-hero-actions { flex-direction: column; align-items: stretch; }
    .gu-hero-actions .btn-premium { width: 100%; justify-content: center; }
    .gu-stats { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .gu-fg--2,
    .gu-fg--3 { grid-template-columns: 1fr; }
    .gu-fg--6 { grid-template-columns: 1fr; }
    .gu-fg--6 .gu-fg-item { grid-column: 1 / -1 !important; }
    .gu-fg--1 .gu-fg-media { min-height: 320px; }
    .gu-fg--1 .gu-fg-media img { max-height: 380px; }
    .gu-fg-media { min-height: 260px; }
    .gu-fg-media img { max-height: 320px; }
    .gu-highlights { grid-template-columns: 1fr; }
    .gu-spec-row { grid-template-columns: 1fr; }
    .gu-spec-row strong { border-right: 0; border-bottom: 1px solid var(--sand); padding-bottom: 12px; }
    .gu-spec-row span { padding-top: 12px; }
    .gu-fact { flex: 1 1 100%; border-right: 0; }
    .gu-ba-stage { border-radius: 18px; }
    .gu-ba-handle-knob { width: 48px; height: 48px; }
    .gu-quote { padding: 72px 20px; }
    .gu-quote-mark { font-size: 5rem; }
  }
</style>
